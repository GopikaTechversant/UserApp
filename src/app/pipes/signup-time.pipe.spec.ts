import { SignupTimePipe } from './signup-time.pipe';

describe('SignupTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SignupTimePipe();
    expect(pipe).toBeTruthy();
  });
});
